/**
 * Affirmation module hooks
 * Contains hooks for the affirmation module
 */

/**
 * Add hooks for the affirmation module
 * @param {Object} ctx - The application context
 * @returns {Object} The application context
 */
export function affirmationHooks(ctx) {
  // Add hooks as needed
  // Example:
  // ctx.hooks.on(HOOK_EVENTS.USER_CREATED, async (user) => {
  //   // Create default affirmations for new users
  // });

  return ctx;
} 